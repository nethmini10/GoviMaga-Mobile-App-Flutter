import 'package:flutter/material.dart';

// ─── Color Palette (55% white / 45% green) ───────────────────────────────────
class AppColors {
  // Whites & Light Neutrals (~55%)
  static const bg = Color(0xFFF0FDF4);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceLight = Color(0xFFECFDF5);
  static const surfaceMid = Color(0xFFD1FAE5);

  // Greens (~45%)
  static const accent = Color(0xFF16A34A);
  static const accentLight = Color(0xFF22C55E);
  static const accentDark = Color(0xFF15803D);
  static const accentDeep = Color(0xFF14532D);
  static const accentGlow = Color(0xFFBBF7D0);
  static const accentBorder = Color(0xFF86EFAC);
  static const accentHero = Color(0xFF4ADE80);

  // Text
  static const textPrimary = Color(0xFF052E16);
  static const textSecondary = Color(0xFF166534);
  static const textMuted = Color(0xFF4ADE80);
  static const textOnGreen = Color(0xFFFFFFFF);

  // Status / Weather
  static const warmOrange = Color(0xFFEA580C);
  static const coolBlue = Color(0xFF0284C7);
  static const alertRed = Color(0xFFDC2626);
  static const alertAmber = Color(0xFFD97706);
  static const alertAmberBg = Color(0xFFFFFBEB);
  static const alertAmberBorder = Color(0xFFFDE68A);

  // Gradients and decoration
  static const gradientStart = Color(0xFF14532D);
  static const gradientEnd = Color(0xFF16A34A);
  static const gradientMid = Color(0xFF15803D);

  // Divider / Shadow
  static const divider = Color(0xFFDCFCE7);
  static const shadowGreen = Color(0x2016A34A);
  static const shadowDeep = Color(0x3014532D);
}
